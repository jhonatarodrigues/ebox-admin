import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#001D7E",
    },
    secondary: {
      main: "#A49171",
    },
  },
});

export const MaterialUiTheme = theme;
